import styled from "styled-components";

const InputDescription = styled.h1`
  font-size: 14px;
  font-weight: 400;
  background-color: white;

  padding-left: 8px;
`;

export default InputDescription;
